name: "CloudRDP [15:35:26] - RDP-INSTANCE-1"
on:
  workflow_dispatch:
jobs:
  rdp:
    runs-on: ubuntu-22.04
    timeout-minutes: 360
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v3

      - name: Setup RDP Environment
        run: |
          export BACKEND_URL="https://silent-numbers-share.loca.lt"
          export INSTANCE_ID="47204fde-38c8-4cd9-ad8c-3b22a01afa1b"
          export NGROK_TOKEN=""
          bash scripts/RDP-INSTANCE-1_setup.sh

      - name: Report RDP Ready
        run: |
          for i in {1..5}; do
            HTTP_STATUS=$(curl -sSL -k -o /dev/null -w "%{http_code}" \
              -X POST "https://silent-numbers-share.loca.lt/instance/47204fde-38c8-4cd9-ad8c-3b22a01afa1b/ready" \
              -H "Content-Type: application/json" \
              -H "Bypass-Tunnel-Reminder: true" \
              -d '{"status": "ready"}')
            echo "Ready report attempt $i: HTTP $HTTP_STATUS"
            if [ "$HTTP_STATUS" -eq 200 ]; then
              echo "✅ Ready signal sent successfully!"
              break
            fi
            sleep 3
          done

      - name: Keep Alive (6 hours)
        run: |
          echo "============================================"
          echo "  CloudRDP Instance Active"
          echo "  Instance: RDP-INSTANCE-1"
          echo "  ID: 47204fde-38c8-4cd9-ad8c-3b22a01afa1b"
          echo "  Session will expire in 6 hours."
          echo "============================================"
          sleep 21600
